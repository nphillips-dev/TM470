@page
@model userListsModel
@{
}


    <div class="container">
        <div class="row">
            <h1>@Model.title</h1>
        </div>
        <div class="row">
            <div class="col">
                <div class="row">
                    <table class="table table-dark table-striped">
                        <thead>
                            <tr>
                                <th>Beer</th>
                                <th>Country</th>
                            </tr>
                        </thead>
                        <tbody>
                                @foreach (var beer in Model.beers)
                                {
                                    <tr>
                                        <td>@beer.name</td>
                                        <td>@beer.Country</td>
                                    </tr>

                                }
                        </tbody>
                    </table>

                </div>

            </div>
        </div>
    </div>